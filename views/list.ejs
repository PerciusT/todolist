<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>List</title>
	<link rel="stylesheet" href="css/styles.css">
</head>
<body>
	<div class="box" id="heading">
		<h1><%= weekday %> List</h1> 
	</div>
	<div class="box">
		<% for (var i in newListItems) { %>
		<form action="/del<%= post %>" method="post">
			<div class="item">
				<input type="checkbox" onChange="this.form.submit()" name="checker" value="<%= newListItems[i]._id %>">
				<p><%= newListItems[i].name %></p>
			</div>
		</form>
		
		<% } %>
		<form class="item" action="/<%= post %>" method="post">
			<input name="newTodo" type="text" id="newTodo" placeholder="New Item">
			<button type="submit" name="button">+</button>
		</form>
	</div>
	
   
	

</body>
</html>